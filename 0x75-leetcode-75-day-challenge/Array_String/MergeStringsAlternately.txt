1. Java Strings
String basics – how to create, store, and manipulate strings.

String methods – .length(), .charAt(int index), .toString().

String immutability – why modifying a String repeatedly is inefficient and why StringBuilder is preferred for concatenation.

2. StringBuilder
Purpose – mutable alternative to String for building/modifying text efficiently.

Common methods – .append(), .insert(), .delete(), .reverse().

Performance – why StringBuilder is faster than using + in loops.

3. Loops
For loops – initialization, condition, increment parts.

Looping over indexes – using i to access elements/characters.

4. Conditional Statements
if statements – basic syntax, conditions inside parentheses.

Short-circuit checks – why we check i < length1 before accessing charAt(i) to avoid IndexOutOfBoundsException.

5. Math Class
Math.max(a, b) – how static utility methods work.

Other common methods (Math.min, Math.abs, etc.).

6. Data Types
Primitive types (int, char) vs Reference types (String, StringBuilder).

Autoboxing/unboxing (optional deeper dive).

7. Defensive Programming
Bounds checking – why you verify indexes before accessing elements.

Avoiding runtime exceptions (StringIndexOutOfBoundsException).


